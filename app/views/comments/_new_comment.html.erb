
	<div class="comment-form">
	    <%= form_for :comment, url:user_activity_page_comments_path(rahul.user, rahul), remote: true, html: {id: "create_comment_form"} do |f| %>
              <%= render partial:'shared/comment_error', locals:{ comment:comment } %>
	        <%= f.text_field :content, placeholder: "comment here" %>
	    <%end %>
	</div>

                 