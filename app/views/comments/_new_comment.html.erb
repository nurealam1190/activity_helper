<div class="hello-comment">
	<div class="comment-form">
	    <%= form_for :comment, url:user_activity_page_comments_path(current_user, rahul), remote: true, html: {id: "create_comment_form"} do |f| %>
             
	        <%= f.text_field :content, placeholder: "comment here" %>
	    <%end %>
	</div>
</div>
                 